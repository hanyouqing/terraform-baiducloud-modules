# BaiduCloud Compute (BCC) Module

Production BCC instances with optional CDS disks and EIP association.

Prefer `instance_spec` (e.g. `bcc.g5.c2m8`) over legacy cpu/memory fields.

## Examples

- [`examples/basic`](examples/basic)
- [`examples/complete`](examples/complete)
