# BaiduCloud RDS Module

Relational Database Service (MySQL / SQLServer / PostgreSQL).

Supports primary instances, read replicas, DB accounts, and security IP whitelists.

- `examples/basic` — single private MySQL
- `examples/complete` — multi-AZ, RO replica, app account, VPC security IPs
